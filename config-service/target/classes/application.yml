server:
  port: 8888

spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          uri: https://github.com/microservices-config/repo
          default-label: main
          skip-ssl-validation: true

management:
  endpoints:
    web:
      exposure:
        include: "*"